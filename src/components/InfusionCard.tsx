import type {CardData} from "../types/types.ts";

interface InfusionProps {
    data: CardData
}

export default function InfusionCard(props: InfusionProps) {
    return (
        <div className="cardwrapper">
            <div className="card">

            </div>
        </div>
    )
};
